# Development automatically uses http://127.0.0.1:3118.
# For a deployment, configure a dedicated demo Laravel/PHP backend before building.
# Never point this to a production application containing real customer data.
# DEMO_BACKEND_URL=https://your-dedicated-demo-backend.example
