@extends('journey.layout')
@section('title',$opportunity->title)
@section('content')<a class="muted" href="/pkl">← Semua peluang</a><div class="hero"><section><span class="eyebrow">{{ $opportunity->category }} / {{ $opportunity->city }}</span><h1>{{ $opportunity->title }}</h1><p class="lead">{{ $opportunity->company }}</p><span class="tag">{{ $opportunity->capacity }} tempat</span> <span class="tag">Batas {{ $opportunity->deadline }}</span><hr class="divider"><h2>Tentang kesempatan ini</h2><div class="prose">{{ $opportunity->description }}</div></section><section class="card"><h2>Mulai dari sini.</h2>@if(!$opportunity->active || $opportunity->deadline < now()->toDateString())<p>Lowongan ini sudah ditutup.</p>@else @auth @if(auth()->user()->role==='student')<p>Ceritakan minatmu dan lampirkan CV PDF maksimal 2 MB.</p><form method="post" action="/pkl/jobs/{{ $opportunity->id }}/apply" enctype="multipart/form-data">@csrf<div class="field"><label for="motivation">Motivasi melamar</label><textarea id="motivation" name="motivation" minlength="20" maxlength="3000" required>{{ old('motivation') }}</textarea></div><div class="field"><label for="cv">CV (PDF, maks. 2 MB)</label><input id="cv" name="cv" type="file" accept="application/pdf" required></div><button class="primary">Kirim lamaran ↗</button></form>@else<p>Anda masuk sebagai pengelola. Gunakan dashboard untuk mengelola lamaran.</p><a class="button" href="/pkl/dashboard">Buka dashboard</a>@endif @else<p>Masuk atau buat akun siswa untuk mengirim lamaran.</p><a class="button" href="/login">Masuk untuk melamar ↗</a>@endauth @endif</section></div>@endsection
