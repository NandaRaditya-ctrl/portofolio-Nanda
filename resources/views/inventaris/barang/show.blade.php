@extends('inventaris.layouts.app')
@section('page-title','Detail Barang')
@section('content')<article class="card card-body max-w-3xl"><span class="text-muted">{{ $barang->kode_barang }}</span><h2 class="text-3xl font-bold my-4">{{ $barang->nama_barang }}</h2><dl class="detail-list">@foreach(['Kategori'=>$barang->kategori->nama_kategori,'Jumlah'=>$barang->jumlah.' unit','Kondisi'=>$barang->kondisi_label,'Lokasi'=>$barang->lokasi,'Tanggal masuk'=>$barang->tanggal_masuk->format('d M Y'),'Keterangan'=>$barang->keterangan] as $label=>$value)<dt>{{ $label }}</dt><dd>{{ $value ?: '—' }}</dd>@endforeach</dl><div class="flex flex-wrap gap-3"><a class="btn btn-primary" href="{{ route('inventaris.barang.edit',$barang) }}">Edit barang</a><a class="btn btn-secondary" href="{{ route('inventaris.barang.index') }}">Kembali</a><form method="post" action="{{ route('inventaris.barang.destroy',$barang) }}" onsubmit="return confirm('Hapus barang ini?')">@csrf @method('DELETE')<button class="btn btn-secondary">Hapus</button></form></div></article>@endsection
