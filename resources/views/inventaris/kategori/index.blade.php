@extends('inventaris.layouts.app')
@section('page-title','Kategori Barang')
@section('content')<div class="flex justify-between items-center mb-3"><h2 class="text-2xl font-bold">Kategori</h2><a class="btn btn-primary" href="{{ route('inventaris.kategori.create') }}">+ Tambah kategori</a></div><div class="grid grid-cols-1 gap-4 md:grid-cols-3">@forelse($kategoris as $kategori)<article class="card card-body"><h3 class="text-xl font-bold">{{ $kategori->nama_kategori }}</h3><p class="text-muted">{{ $kategori->deskripsi }}</p><p class="my-3">{{ $kategori->barangs_count }} jenis barang</p><div class="flex gap-2"><a class="btn btn-secondary" href="{{ route('inventaris.kategori.edit',$kategori) }}">Edit</a><form method="post" action="{{ route('inventaris.kategori.destroy',$kategori) }}" onsubmit="return confirm('Hapus kategori ini?')">@csrf @method('DELETE')<button class="btn btn-secondary">Hapus</button></form></div></article>@empty<p>Belum ada kategori.</p>@endforelse</div>@endsection
