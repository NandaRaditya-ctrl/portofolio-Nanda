@extends('inventaris.layouts.app')
@section('page-title','Form Kategori')
@section('content')<form class="card card-body max-w-2xl" method="post" action="{{ isset($kategori) ? route('inventaris.kategori.update',$kategori) : route('inventaris.kategori.store') }}">@csrf @isset($kategori) @method('PUT') @endisset<h2 class="text-xl font-bold mb-4">{{ isset($kategori) ? 'Edit' : 'Tambah' }} kategori</h2>@include('inventaris.errors')<label class="block mb-2" for="nama">Nama kategori</label><input class="form-control w-full mb-4 border rounded-lg p-3" id="nama" name="nama_kategori" maxlength="255" value="{{ old('nama_kategori',$kategori->nama_kategori ?? '') }}" required><label class="block mb-2" for="deskripsi">Deskripsi</label><textarea class="form-control w-full border rounded-lg p-3" id="deskripsi" name="deskripsi" maxlength="500">{{ old('deskripsi',$kategori->deskripsi ?? '') }}</textarea><div class="flex gap-3 mt-4"><button class="btn btn-primary">Simpan</button><a class="btn btn-secondary" href="{{ route('inventaris.kategori.index') }}">Batal</a></div></form>@endsection
